# คู่มือการติดตั้งโปรเจ็กต์บน Alwaysdata อย่างละเอียด (Deployment Guide)

คู่มือนี้จะแนะนำขั้นตอนการนำโปรเจ็กต์ **ระบบจัดการหอพัก (Rental Management System)** ขึ้นไปติดตั้งและรันบนโฮสติ้ง **Alwaysdata** แบบทีละขั้นตอน (Step-by-Step) เพื่อให้คุณตั้งค่าระบบได้อย่างสมบูรณ์และมีความปลอดภัยสูง

---

## สารบัญ
1. [การเตรียมตัวและสมัครใช้งาน Alwaysdata](#1-การเตรียมตัวและสมัครใช้งาน-alwaysdata)
2. [การสร้างฐานข้อมูล MySQL บน Alwaysdata](#2-การสร้างฐานข้อมูล-mysql-บน-alwaysdata)
3. [การตั้งค่าเว็บไซต์ (Website Configuration) และการกำหนดค่าฐานข้อมูล](#3-การตั้งค่าเว็บไซต์-website-configuration-และการกำหนดค่าฐานข้อมูล)
4. [การอัปโหลดโค้ดขึ้นระบบ (SFTP / Git)](#4-การอัปโหลดโค้ดขึ้นระบบ-sftp--git)
5. [การติดตั้ง Dependencies ด้วย Composer ผ่าน SSH](#5-การติดตั้ง-dependencies-ด้วย-composer-ผ่าน-ssh)
6. [การเริ่มต้นฐานข้อมูล (Database Setup)](#6-การเริ่มต้นฐานข้อมูล-database-setup)
7. [การเพิ่มความปลอดภัยหลังติดตั้งเสร็จสิ้น](#7-การเพิ่มความปลอดภัยหลังติดตั้งเสร็จสิ้น)

---

## 1. การเตรียมตัวและสมัครใช้งาน Alwaysdata
Alwaysdata เป็นบริการ Web Hosting ที่รองรับ PHP, Node.js, Python และมีพื้นที่ให้ใช้งานฟรี 100 MB พร้อมสิทธิ์การเข้าถึง SSH, Database และ FTP
1. เข้าไปที่ [alwaysdata.com](https://www.alwaysdata.com/) แล้วสมัครสมาชิก (แบบ Free Cloud)
2. เมื่อลงทะเบียนเรียบร้อย ระบบจะให้สิทธิ์ใช้งานบัญชีของคุณ โดยจะมี URL เริ่มต้นเป็น `https://[ชื่อบัญชี].alwaysdata.net/`
3. เข้าสู่ระบบ **Control Panel** เพื่อเริ่มดำเนินการในขั้นตอนถัดไป

---

## 2. การสร้างฐานข้อมูล MySQL บน Alwaysdata
เนื่องจากบน Alwaysdata สิทธิ์การจัดการฐานข้อมูลจะจำกัดเฉพาะฐานข้อมูลที่เริ่มต้นด้วยชื่อบัญชีของคุณ (เช่น `username_dbname`) จึงต้องสร้างผ่านหน้า Control Panel ก่อน

1. ในหน้า Control Panel ของ Alwaysdata ไปที่เมนู **Databases** -> **MySQL** ด้านซ้ายมือ
2. คลิกปุ่ม **Add a database**
   - **Name:** ระบุชื่อต่อท้าย เช่น `rental` (ชื่อเต็มจะเป็น `[ชื่อบัญชี]_rental` เช่น `myaccount_rental`)
   - **Character set:** เลือก `utf8mb4_unicode_ci` (เพื่อให้รองรับภาษาไทยอย่างถูกต้อง)
   - คลิก **Submit**
3. ในส่วน **MySQL Users** (หากยังไม่มี User) หรือต้องการสร้าง User ใหม่สำหรับโปรเจ็กต์นี้:
   - คลิก **Add a user**
   - **Username:** ตั้งชื่อ User เช่น `rental_user` (ชื่อเต็มจะเป็น `[ชื่อบัญชี]_rental_user`)
   - **Password:** กำหนดรหัสผ่านที่ปลอดภัย (จดบันทึกรหัสผ่านนี้ไว้)
   - **Permissions:** ติ๊กเลือกฐานข้อมูลที่คุณเพิ่งสร้างขึ้น เพื่อให้สิทธิ์ในการเขียน/อ่าน
   - คลิก **Submit**
4. **จดบันทึกข้อมูลเพื่อนำไปใช้ในไฟล์คอนฟิก:**
   - **Database Host:** โดยปกติจะเป็น `mysql-[ชื่อบัญชี].alwaysdata.net` (แสดงอยู่ในหน้า MySQL ของ Control Panel)
   - **Database Name:** `[ชื่อบัญชี]_rental`
   - **Username:** `[ชื่อบัญชี]_rental_user`
   - **Password:** รหัสผ่านที่คุณตั้งค่าไว้

---

## 3. การตั้งค่าเว็บไซต์ (Website Configuration) และการกำหนดค่าฐานข้อมูล

### วิธีแก้ไขที่ดีที่สุดและง่ายที่สุด: ใช้ไฟล์ `.htaccess`
เราสามารถกำหนดค่า Environment Variables ให้กับ PHP ผ่านคำสั่ง `SetEnv` ในไฟล์ `.htaccess` ได้โดยตรง ซึ่งเป็นฟังก์ชันมาตรฐานของเว็บเซิร์ฟเวอร์ Apache บน Alwaysdata

1. **สร้างไฟล์และเตรียมอัปโหลดเว็บไซต์:**
   - ไปที่เมนู **Websites** ใน Control Panel ของ Alwaysdata
   - หากมีเว็บไซต์เดิมอยู่แล้ว ให้คลิกปุ่มดินสอ (**Edit**) หรือหากยังไม่มีให้คลิก **Add a Website**
   - **Name:** ตั้งชื่อเว็บไซต์ของคุณ (เช่น Rental App)
   - **Addresses:** กำหนด Domain ที่ใช้ (เช่น `[ชื่อบัญชี].alwaysdata.net` หรือโดเมนส่วนตัวของคุณ)
   - **Type:** เลือก **PHP**
   - **PHP Version:** เลือกเวอร์ชันล่าสุด (แนะนำ **8.1**, **8.2** หรือ **8.3**)
   - **Directory:** ระบุโฟลเดอร์ปลายทางที่เก็บโปรเจ็กต์ (เช่น `/www`)
   - คลิก **Submit** เพื่อบันทึกการตั้งค่าเว็บไซต์

2. **ใช้ไฟล์ `.htaccess.example` ในโปรเจ็กต์:**
   - ในโฟลเดอร์หลักของโปรเจ็กต์นี้ ผมได้สร้างไฟล์ต้นแบบชื่อ [`.htaccess.example`](file:///c:/xampp/htdocs/Rental/.htaccess.example) ไว้ให้แล้ว
   - ให้เปิดไฟล์นี้และแก้ไขค่าเชื่อมต่อฐานข้อมูลตามข้อมูลจริงที่คุณสร้างไว้ในขั้นตอนที่ 2
   - เมื่ออัปโหลดไฟล์นี้ขึ้นโฮสต์ Alwaysdata แล้ว ให้เปลี่ยนชื่อไฟล์นี้จาก `.htaccess.example` เป็น **`.htaccess`** (เริ่มด้วยเครื่องหมายจุด)
   
   *ตัวอย่างการเขียนในไฟล์ `.htaccess`:*
   ```apache
   SetEnv RENTAL_DB_HOST "mysql-[ชื่อบัญชี].alwaysdata.net"
   SetEnv RENTAL_DB_USER "[ชื่อบัญชี]_rental_user"
   SetEnv RENTAL_DB_PASSWORD "[รหัสผ่านของคุณ]"
   SetEnv RENTAL_DB_NAME "[ชื่อบัญชี]_rental"
   SetEnv RENTAL_BASE_URL "/"
   SetEnv RENTAL_ALLOW_SETUP "1"
   SetEnv RENTAL_SETUP_TOKEN "mysecrettoken123"
   SetEnv RENTAL_ADMIN_PASSWORD "admin1234"
   ```
   *(หมายเหตุ: แนะนำให้ทำขั้นตอนนี้บนเซิร์ฟเวอร์เพื่อไม่ให้รบกวนข้อมูลเชื่อมต่อฐานข้อมูล `localhost` เดิมที่คุณใช้พัฒนาในเครื่องคอมพิวเตอร์)*

   **สรุปว่าต้องแก้ตรงไหน**
   - ถ้าจะแก้แล้ว commit ขึ้น git ให้แก้ที่ [`.htaccess.example`](file:///c:/xampp/htdocs/Rental/.htaccess.example)
   - ถ้าอัปโหลดขึ้น Alwaysdata แล้ว ให้แก้ที่ไฟล์ `/www/.htaccess` บนเซิร์ฟเวอร์ผ่าน SSH หรือ Git Bash
   - ค่าที่ต้องเปลี่ยนให้เป็นค่าจริงของคุณคือ `RENTAL_DB_HOST`, `RENTAL_DB_USER`, `RENTAL_DB_PASSWORD`, `RENTAL_DB_NAME`, `RENTAL_BASE_URL`, `RENTAL_APP_HOST`, `RENTAL_APP_SCHEME`
   - ค่าที่ใช้เฉพาะตอนติดตั้งครั้งแรกคือ `RENTAL_ALLOW_SETUP`, `RENTAL_SETUP_TOKEN`, `RENTAL_ADMIN_PASSWORD`

---

## 4. การอัปโหลดโค้ดขึ้นระบบ (SFTP / Git)
คุณสามารถเลือกวิธีการอัปโหลดได้ 2 รูปแบบตามความสะดวก:

### ทางเลือกที่ 1: อัปโหลดผ่าน SFTP (สะดวกที่สุดสำหรับมือใหม่)
1. ไปที่เมนู **Services** -> **FTP/SFTP** ใน Control Panel เพื่อดู Hostname และ Username
2. ดาวน์โหลดและเปิดโปรแกรม **FileZilla**
3. เชื่อมต่อด้วยค่าดังนี้:
   - **Host:** `sftp://ssh-[ชื่อบัญชี].alwaysdata.net`
   - **Username:** `[ชื่อบัญชี]`
   - **Password:** รหัสผ่านบัญชี Alwaysdata ของคุณ
   - **Port:** `22` (หรือปล่อยว่างไว้)
4. เมื่อเชื่อมต่อสำเร็จ ให้เปิดโฟลเดอร์ปลายทาง (เช่น `/www`) จากนั้นลากไฟล์และโฟลเดอร์ของโปรเจ็กต์นี้ทั้งหมดจากเครื่องของคุณไปวาง
   > [!IMPORTANT]
   **ห้ามลากโฟลเดอร์ `vendor` ขึ้นไป** เพราะในขั้นตอนถัดไปเราจะติดตั้งผ่านคำสั่ง SSH บนเซิร์ฟเวอร์โดยตรงเพื่อป้องกันปัญหาไฟล์ตกหล่นและประสิทธิภาพที่ดีกว่า

### ทางเลือกที่ 2: อัปโหลดผ่าน Git (แนะนำสำหรับนักพัฒนา)
Alwaysdata รองรับ Git สำเร็จรูป คุณสามารถทำตามนี้ได้:
1. ไปที่เมนู **Services** -> **SSH** เพื่อเปิดใช้งานการเชื่อมต่อ SSH
2. เปิด Terminal (บน Windows ใช้ PowerShell หรือ Git Bash) แล้วเชื่อมต่อไปยังเซิร์ฟเวอร์:
   ```bash
   ssh [ชื่อบัญชี]@ssh-[ชื่อบัญชี].alwaysdata.net
   ```
3. เมื่อเข้าสู่ระบบแล้ว ให้เข้าไปที่โฟลเดอร์เว็บ:
   ```bash
   cd www
   ```
4. โคลนโปรเจ็กต์ของคุณมาจาก GitHub/GitLab:
   ```bash
   git clone <URL_โปรเจ็กต์ของคุณ> .
   ```

---

## 5. การติดตั้ง Dependencies ด้วย Composer ผ่าน SSH
เนื่องจากโปรเจ็กต์ต้องการ Library เช่น `PhpSpreadsheet` และ `PHPMailer` เราจึงต้องรัน Composer เพื่อติดตั้งไฟล์ในโฟลเดอร์ `vendor`

1. เชื่อมต่อ SSH เข้าไปยังเซิร์ฟเวอร์ Alwaysdata:
   ```bash
   ssh [ชื่อบัญชี]@ssh-[ชื่อบัญชี].alwaysdata.net
   ```
2. ย้ายไปยังโฟลเดอร์ที่เก็บไฟล์ของเว็บไซต์ (เช่น `/www`):
   ```bash
   cd www
   ```
3. สั่งติดตั้ง Composer Dependencies สำหรับสภาพแวดล้อม Production:
   ```bash
   composer install --no-dev --optimize-autoloader
   ```

---

## 6. การเริ่มต้นฐานข้อมูล (Database Setup)
โปรเจ็กต์นี้มีสคริปต์ช่วยติดตั้งฐานข้อมูลสำเร็จรูปอยู่ใน `database/setup.php` แต่เนื่องจากการรันผ่านเว็บอาจจะติดปัญหาที่ `setup.php` พยายามสั่ง `CREATE DATABASE` อีกครั้ง (ซึ่ง MySQL บน Alwaysdata อาจบล็อกสิทธิ์นี้) จึงมีทางเลือกให้ดำเนินการดังนี้:

### ทางเลือกที่ 1: ติดตั้งผ่านสคริปต์ `setup.php` (วิธีอัตโนมัติ)
1. เปิดเว็บเบราว์เซอร์แล้วพิมพ์ URL ของโปรเจ็กต์คุณ ตามด้วยโทเค็นความปลอดภัยที่ตั้งไว้ในขั้นตอนที่ 3:
   ```
   https://[ชื่อบัญชี].alwaysdata.net/database/setup.php?setup_token=mysecrettoken123
   ```
2. หากสิทธิ์ของฐานข้อมูลผ่าน ระบบจะสร้างตารางและสร้างบัญชี Admin ให้ทันที พร้อมแสดงรหัสผ่านแรกเริ่มบนหน้าจอ
3. หากระบบแจ้งเตือนว่าไม่สามารถสร้างฐานข้อมูลได้ (เพราะฐานข้อมูลถูกสร้างล่วงหน้าไปแล้วในขั้นตอนที่ 2) ให้ทำตาม **ทางเลือกที่ 2** ด้านล่าง

### ทางเลือกที่ 2: ติดตั้งด้วยการ Import ผ่าน phpMyAdmin (แนะนำและชัวร์ที่สุด)
1. ดาวน์โหลดไฟล์ `database/schema.sql` มาเก็บไว้ที่เครื่องคอมพิวเตอร์ของคุณ
2. ใช้ Text Editor (เช่น VS Code / Notepad) เปิดไฟล์ `schema.sql`
3. ค้นหาและ**ลบบรรทัดที่ 3 และ 4** ออก หรือใส่เครื่องหมายคอมเมนต์ `--` ไว้ข้างหน้า ดังนี้:
   ```sql
   -- CREATE DATABASE IF NOT EXISTS rental_db CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
   -- USE rental_db;
   ```
4. บันทึกและปิดไฟล์
5. ไปที่ Control Panel ของ Alwaysdata เมนู **Databases** -> **MySQL** แล้วคลิกลิงก์ **phpMyAdmin** ที่มุมบนขวา
6. เลือกชื่อฐานข้อมูลของคุณในแถบด้านซ้าย (เช่น `[ชื่อบัญชี]_rental`)
7. คลิกแท็บ **Import** (นำเข้า) ด้านบน
8. คลิก **Choose File** แล้วเลือกไฟล์ `schema.sql` ที่คุณเพิ่งแก้ไขเสร็จ จากนั้นเลื่อนลงมาคลิกปุ่ม **Import** (นำเข้า) ที่ด้านล่างสุด
9. ทำซ้ำขั้นตอนเดิมเพื่อ Import ไฟล์ตัวอย่าง (ถ้าต้องการ):
   - Import ไฟล์ `database/seed.sql` (เพื่อเพิ่มข้อมูลตัวอย่างประเภทห้องและสิทธิ์การเข้าใช้งานเบื้องต้น)
   - Import ไฟล์ `database/performance_indexes.sql` (เพื่อทำดัชนีเพิ่มความเร็วของฐานข้อมูล)

#### หมายเหตุ: กรณี Import ด้วยมือ คุณต้องตรวจสอบว่ามี User แอดมินในตาราง `users` แล้วหรือยัง
หากไม่มีแอดมิน ให้ไปที่ phpMyAdmin แท็บ **SQL** แล้วรันคำสั่ง SQL นี้เพื่อสร้างผู้ใช้ Admin หลัก (รหัสผ่านเริ่มต้นจะเป็น `admin1234`):
```sql
INSERT INTO users (username, password, email, full_name, role, is_active) VALUES 
('admin', '$2y$10$UoWpYskjC4d6yP9PqDkUOu6/7m96TeeUeE.gqL537/r8ZlV6kFm7e', 'admin@dormitory.com', 'Administrator', 'admin', 1);
```

---

## 7. การเพิ่มความปลอดภัยหลังติดตั้งเสร็จสิ้น
หลังจากที่คุณทดสอบเข้าระบบได้เรียบร้อยแล้ว (`Username: admin`) จำเป็นต้องดำเนินมาตรการรักษาความปลอดภัยทันที:

1. **ลบหรือย้ายไฟล์ติดตั้ง:**
   เข้า SFTP หรือ SSH ไปที่โฟลเดอร์ `database/` จากนั้นสั่งลบไฟล์ `setup.php` ทิ้งทันที เพื่อป้องกันบุคคลอื่นมารันระบบติดตั้งใหม่ทับฐานข้อมูลเดิม
   ```bash
   rm database/setup.php
   ```
2. **ปิดสิทธิ์การลงทะเบียนและตั้งค่าใหม่ในไฟล์ `.htaccess`:**
   เปิดไฟล์ `/www/.htaccess` บนเซิร์ฟเวอร์ผ่าน SSH หรือ Git Bash แล้วทำการลบ (หรือคอมเมนต์ `#`) บรรทัดเหล่านี้ทิ้งเพื่อป้องกันไม่ให้ผู้อื่นรันสคริปต์ setup ได้อีก:
   ```apache
   # ทำการลบหรือใส่คอมเมนต์ # บรรทัดด้านล่างนี้
   # SetEnv RENTAL_ALLOW_SETUP "1"
   # SetEnv RENTAL_SETUP_TOKEN "mysecrettoken123"
   ```

เป็นอันเสร็จสิ้นขั้นตอนการติดตั้งโปรเจ็กต์ **ระบบจัดการหอพัก** บน Alwaysdata อย่างสมบูรณ์! คุณสามารถเข้าใช้งานผ่าน Domain ของคุณได้ทันที
